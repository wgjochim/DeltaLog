<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DeltaLog" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="AlerdialogBackground" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/foreground_gray</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/hint_gray</item>
        <item name="colorAccent">@color/white</item>
    </style>

</resources>